#include"func.h"
int flag=0;

int main() {
int d;	
initialization();
while (1)
{
	delay_1sec();
	//read function
	//trimmed func
	if(flag==0){
	
		//prev_values
		flag=1;}
	else if(flag==1){
	//d=distance(prev_values[0],prev_values[1],,)}
		if(d==-1){
		skip;}
		else { led(d);
		      segment(d);}
		if(d>100){
		break;}
}
	}
